{
  "name": "ghost-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "1.0.0-beta.127",
    "har-to-openapi": "^2.0.0"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle -o dist/bundle.yaml",
    "lint": "redocly lint",
    "convert": "node lib/converter.js",
    "generate": "npm run test && npm run convert",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.1",
    "dotenv": "^16.3.1"
  },
  "type": "module"
}
